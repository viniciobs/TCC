<%= form_with(model: user, local: true, html: { multipart: true }) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "erro") %>:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-row">
     <div class="form-group col-lg-12 col-md-12 col-sm-12 col-xs-12">
  <div class="field">
    Nome: <%= form.label user.name %>  
  </div>
</div>
  </div>

<div class="form-row">    
<div class="form-group col-lg-12 col-md-12 col-sm-12 col-xs-12">
  <div class="field">
    <br/>
    <%= form.label :active, "Ativo?" %>
    <%= form.check_box :active, class: "form-check-input" %>
  </div>
</div>
</div>

  <div class="form-row">    
<div class="form-group col-lg-12 col-md-12 col-sm-12 col-xs-12">
  <div class="field">
    <%= form.label :user_type, "Tipo de usuário" %>
    <%= form.select(:user_type, options_for_select([['-- Selecione o tipo --', nil], ['Admin', 'manager'], ['Artista', 'musician'], ['Cliente', 'customer']], :selected => user.user_type), {}, {class: "form-control"})  %>
  </div>
</div>
</div>

  <div class="form-row">
  <div class="form-group col-lg-12 col-md-12 col-sm-12 col-xs-12 actions">
    <%= form.submit "Salvar", class: "btn btn-orange right" %>
    <%= link_to 'Cancelar', users_path, class: "right btn btn-orange" %>
  </div>
</div>
<% end %>
